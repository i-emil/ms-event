spring:
  pagination:
    max-size: 10
  datasource:
    url: jdbc:postgresql://postgres-postgresql.postgres.svc.cluster.local:5432/ms_tag
    username: postgres
    password: P@ssw0rD
  liquibase:
    enabled: true
    change-log: classpath:liquibase/changelog-master.yml
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
server:
  port: 8085
logging:
  my-level: INFO